{{/*
Opzionale e disabilitato di default.
*/}}
{{- if .Values.namespace.create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace.name }}
  labels:
    {{- include "habit-tracker.labels" . | nindent 4 }}
{{- end }}