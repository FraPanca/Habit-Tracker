# Da utilizzare solo in locale.
#
# Uso:
# 1. cp values-secret.yaml.example values-secret.yaml
# 2. Sostituire username/password con credenziali reali
# 3. helm install habit-tracker ./charts/habit-tracker -f values-dev.yaml -f values-secret.yaml

mongodb:
  auth:
    username: admin
    password: ChangeMe